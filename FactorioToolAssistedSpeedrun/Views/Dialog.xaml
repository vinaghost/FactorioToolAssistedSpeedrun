<Window
    x:Class="FactorioToolAssistedSpeedrun.Views.Dialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FactorioToolAssistedSpeedrun.ViewModels"
    Title="Go To Line"
    d:DataContext="{d:DesignInstance vm:DialogViewModel,
                                     IsDesignTimeCreatable=True}"
    ContentRendered="Window_ContentRendered"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Content="{Binding MinMaxText}"
            ContentStringFormat="Go to line {0}:" />

        <TextBox
            x:Name="LineTxtBox"
            Grid.Row="1"
            MinWidth="250"
            Text="{Binding Line, UpdateSourceTrigger=PropertyChanged}" />
        <WrapPanel
            Grid.Row="2"
            Margin="0,15,0,0"
            HorizontalAlignment="Right">
            <Button
                MinWidth="60"
                Margin="0,0,10,0"
                Click="OkClick"
                Content="_Ok"
                IsDefault="True" />

            <Button
                MinWidth="60"
                Content="_Cancel"
                IsCancel="True" />
        </WrapPanel>
    </Grid>
</Window>