<Window
    x:Class="FactorioToolAssistedSpeedrun.Views.GoToLineWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FactorioToolAssistedSpeedrun.ViewModels"
    Title="Go To Line"
    d:DataContext="{d:DesignInstance vm:GoToLineViewModel,
                                     IsDesignTimeCreatable=True}"
    ContentRendered="Window_ContentRendered"
    Icon="pack://application:,,,/favicon.ico"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Content="{Binding MaxLine}"
            ContentStringFormat="Go to line (1 - {0}):" />

        <TextBox
            x:Name="LineTxtBox"
            Grid.Row="1"
            MinWidth="250"
            Text="{Binding Line, UpdateSourceTrigger=PropertyChanged}" />
        <WrapPanel
            Grid.Row="2"
            Margin="0,15,0,0"
            HorizontalAlignment="Right">
            <Button
                MinWidth="60"
                Margin="0,0,10,0"
                Command="{Binding OKCommand}"
                Content="_Ok"
                IsDefault="True" />

            <Button
                MinWidth="60"
                Command="{Binding CancelCommand}"
                Content="_Cancel" />
        </WrapPanel>
    </Grid>
</Window>