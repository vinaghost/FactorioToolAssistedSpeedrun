<UserControl
    x:Class="FactorioToolAssistedSpeedrun.Views.StepPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attachedBehaviours="clr-namespace:FactorioToolAssistedSpeedrun.Misc.AttachedBehaviours"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:conventers="clr-namespace:FactorioToolAssistedSpeedrun.Misc.Conventers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FactorioToolAssistedSpeedrun.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:misc="clr-namespace:FactorioToolAssistedSpeedrun.Misc"
    xmlns:vm="clr-namespace:FactorioToolAssistedSpeedrun.ViewModels"
    d:DataContext="{d:DesignInstance vm:StepPanelViewModel,
                                     IsDesignTimeCreatable=True}"
    Loaded="LoadHandler"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conventers:StepTypeToStringConverter x:Key="StepTypeToStringConverter" />
        <conventers:StepToColorConverter x:Key="StepToColorConverter" />
        <conventers:ItemNameToStringConverter x:Key="ItemNameToStringConverter" />
        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="attachedBehaviours:MouseRightButtonUpBehavior.MouseRightButtonUpCommand" Value="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}, Path=DataContext.MouseRightButtonUpCommand}" />
            <Setter Property="attachedBehaviours:DeleteRowBehavior.DeleteRowCommand" Value="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}, Path=DataContext.RemoveCommand}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button
            Grid.Column="0"
            Margin="2"
            Content="Add">
            <Button.InputBindings>
                <MouseBinding
                    Command="{Binding AddCommand}"
                    CommandParameter="{StaticResource False}"
                    Gesture="LeftClick" />
                <MouseBinding
                    Command="{Binding AddCommand}"
                    CommandParameter="{StaticResource True}"
                    Gesture="RightClick" />
            </Button.InputBindings>
        </Button>
        <Button
            Grid.Column="1"
            Margin="2"
            Command="{Binding RemoveCommand}"
            CommandParameter="{Binding ElementName=Steps, Path=SelectedItems}"
            Content="Remove" />
        <Button
            Grid.Column="2"
            Margin="2"
            Content="Move Up">
            <Button.InputBindings>
                <MouseBinding
                    Command="{Binding MoveUpOneCommand}"
                    CommandParameter="{Binding ElementName=Steps, Path=SelectedItems}"
                    Gesture="LeftClick" />
                <MouseBinding
                    Command="{Binding MoveUpFiveCommand}"
                    CommandParameter="{Binding ElementName=Steps, Path=SelectedItems}"
                    Gesture="RightClick" />
            </Button.InputBindings>
        </Button>
        <Button
            Grid.Column="3"
            Margin="2"
            Content="Move Down">
            <Button.InputBindings>
                <MouseBinding
                    Command="{Binding MoveDownOneCommand}"
                    CommandParameter="{Binding ElementName=Steps, Path=SelectedItems}"
                    Gesture="LeftClick" />
                <MouseBinding
                    Command="{Binding MoveDownFiveCommand}"
                    CommandParameter="{Binding ElementName=Steps, Path=SelectedItems}"
                    Gesture="RightClick" />
            </Button.InputBindings>
        </Button>
        <Button
            Grid.Column="5"
            Margin="2"
            Command="{Binding SkipCommand}"
            CommandParameter="{Binding ElementName=Steps, Path=SelectedItems}"
            Content="Skip" />
        <DataGrid
            x:Name="Steps"
            Grid.Row="1"
            Grid.ColumnSpan="7"
            Margin="0,5,0,0"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserReorderColumns="False"
            CanUserResizeRows="False"
            CanUserSortColumns="False"
            EnableRowVirtualization="True"
            ItemsSource="{Binding PanelService.StepCollection, Mode=OneWay}"
            RowHeight="20"
            ScrollViewer.IsDeferredScrollingEnabled="True"
            SelectedIndex="{Binding PanelService.SelectedStepIndex}"
            SelectedItem="{Binding PanelService.SelectedStep}"
            SelectionMode="Extended"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Resources>
                <misc:BindingProxy x:Key="proxy" Data="{Binding StartupService.ItemsCollection, Mode=OneWay}" />
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Location, Mode=OneWay}"
                    Header="" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Type, Mode=OneWay, Converter={StaticResource StepTypeToStringConverter}}"
                    Header="Step">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="{x:Type DataGridCell}">
                            <Setter Property="Background" Value="{Binding ., Converter={StaticResource StepToColorConverter}}" />
                            <Style.Triggers>
                                <!--  Trigger for when the cell is selected  -->
                                <Trigger Property="IsSelected" Value="True">
                                    <!--  Change the Foreground to a color that contrasts well with the default selection background (or your custom selection background)  -->
                                    <Setter Property="Foreground" Value="White" />
                                    <!--  Optionally, change the selection background color here if needed  -->
                                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding X}"
                    Header="X" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Y}"
                    Header="Y" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Amount}"
                    Header="Amount" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding Item}"
                    Header="Item">
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="{x:Type TextBox}">
                            <Setter Property="attachedBehaviours:AutoCompleteBehavior.AutoCompleteItemsSource" Value="{Binding Data, Source={StaticResource proxy}}" />
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Orientation}"
                    Header="Orientation" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Modifier}"
                    Header="Modifier" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Comment}"
                    Header="Comment" />
                <DataGridCheckBoxColumn
                    Width="Auto"
                    Binding="{Binding IsSkip}"
                    Header="Skip" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>